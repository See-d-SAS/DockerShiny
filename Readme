# FIrst Install docker
# the the shiny app must be in a repo named "src" in the same location of the dockerfile

docker build -t MyShinyAppName .

docker run  -d -p 80:80 -t MyShinyAppName

# Open in a browser :
http://127.0.0.1/
